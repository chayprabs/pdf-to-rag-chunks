## Summary

<!-- What does this PR change? -->

## Checklist

- [ ] `pnpm lint` and `pnpm typecheck` pass
- [ ] Worker tests pass (`cd apps/worker && PYTHONPATH=src python -m pytest`)
- [ ] No PDF content or secrets in logs
